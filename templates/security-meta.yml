# Copy into your app repo as .github/workflows/security-meta.yml
# Replace OWNER/scankit and prefer a commit SHA instead of @main.
name: Security — meta

on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read
  id-token: write
  pull-requests: read
  packages: read

concurrency:
  group: security-meta-${{ github.ref }}
  cancel-in-progress: true

jobs:
  meta:
    uses: OWNER/scankit/.github/workflows/reusable-meta.yml@c7197e7783122e34257224c14b355c92a9d1450e # pin to commit SHA; bump when upgrading
    with:
      selection-mode: detected
      fail-on-severity: HIGH
      # scan-scope defaults to full for category-only entrypoints;
      # use reusable-security-full for PR diff scoping (scan-scope: auto).
      # Set enable-*: false to disable individual tools in the reusable workflow.
