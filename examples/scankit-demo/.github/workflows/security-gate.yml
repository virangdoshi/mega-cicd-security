name: Security (gate)

on:
  workflow_dispatch:
  pull_request:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write
  security-events: write
  actions: read
  id-token: write
  packages: read

jobs:
  security:
    if: github.event_name == 'workflow_dispatch' || github.event.label.name == 'run-security-gate'
    uses: virangdoshi/scankit/.github/workflows/reusable-security-full.yml@c7197e7783122e34257224c14b355c92a9d1450e
    with:
      profile: minimal
      selection-mode: detected
      fail-on-severity: HIGH
      pr-report-mode: comment
      results-publish-mode: none
